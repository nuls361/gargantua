# Copy to .env for local runs; on Railway these go in the Variables tab (never in git).
TIKHUB_API_KEY=
ANTHROPIC_API_KEY=

# Supabase = lbug project (the CRM + creator index). Service_role key bypasses RLS.
SUPABASE_URL=https://lbugisvvafrhdoyyabtq.supabase.co
SUPABASE_SECRET_KEY=

# Worker tuning
WORKER_POLL_SECONDS=15
AUTONOMOUS_DAILY_USD=0        # 0 = autonomous loop off; e.g. 20 = €20/day cap (Phase 5)
